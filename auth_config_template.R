# PI System Authentication Credentials
# このファイルを 'auth_config.R' にリネームして、ユーザー名とパスワードを設定してください。
# Rename this file to 'auth_config.R' and fill in your credentials.
# 注意: 'auth_config.R' はGitにコミットしないでください。
# Note: Do not commit 'auth_config.R' to version control.

AUTH_USER <- "YOUR_USERNAME"
AUTH_PASS <- "YOUR_PASSWORD"
